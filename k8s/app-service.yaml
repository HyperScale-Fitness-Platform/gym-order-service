apiVersion: v1
kind: Service
metadata:
  name: gym-order-service
  namespace: gym-dev
  labels: { app: gym-order-service }
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
  selector: { app: gym-order-service }
